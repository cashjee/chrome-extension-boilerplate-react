import { resolveMotionValue } from "framer-motion";

export const printLine = (line) => {
  console.log('===> FROM THE PRINT MODULE:', line);
};

// chrome.storage.sync.get('userEmail', function(data){
//   const userEmail = data.userEmail
//   console.log(userEmail)
//   const currentTime = (new Date()).toJSON();
  // const url=new URL(document.documentURI)
//   chrome.runtime.sendMessage({email: userEmail, time: currentTime, url: url, host: url.hostname}, function(response) {
//       console.log(`message from background: `);
//     });

// })



